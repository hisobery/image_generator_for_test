# image_generator_for_test
该脚本通过调用图像生成接口（gpt-image-2 模型），将文本提示词和可选的参考图片生成新图像，并将返回的图片自动下载保存到本地。 主要步骤包括：  将本地参考图片读取并编码为 Base64 字符串； 组装请求体，指定模型、提示词、画面比例、质量等参数； 发送 POST 请求，解析 JSON 响应，提取生成图片的 URL； 下载图片并以 image_时间戳.png 格式保存到脚本同目录下的 generated_images 文件夹中。
